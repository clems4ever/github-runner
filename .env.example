# Repository or organisation the runner registers against.
GITHUB_URL=https://github.com/clems4ever/runyard

# Registration token copied from Settings > Actions > Runners > New self-hosted
# runner (the `--token` value in the snippet GitHub shows there). It is only
# used to register: the runner then has credentials of its own, kept in the
# runner-state volume, so restarts work after the token has expired.
RUNNER_TOKEN=

# Leave empty to use the container hostname (required when scaling).
RUNNER_NAME=
RUNNER_LABELS=self-hosted-docker
RUNNER_GROUP=Default

# true: accept a single job then exit; `restart: unless-stopped` brings up a
# fresh registration, which gives every job a clean workspace. Registering
# again needs a valid token, and a registration token expires one hour after
# it is issued — so this mode means pasting a new token every hour.
EPHEMERAL=false

# The runner auto-updates itself by default; set to true to pin the image
# version instead.
DISABLE_UPDATE=false

# Tag of ghcr.io/clems4ever/github-runner to run: "latest", a runner version
# such as "2.336.0", or a commit sha.
IMAGE_TAG=latest

# gid of the docker group on the host, only needed when mounting docker.sock
# (`getent group docker | cut -d: -f3`)
# DOCKER_GID=988
